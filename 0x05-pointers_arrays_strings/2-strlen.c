#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: string to get length for
 *
 * Return: lenght of string
 */
int _strlen(char *s)
{
	int len;

	len = strlen(*s);

	return (len);
}
