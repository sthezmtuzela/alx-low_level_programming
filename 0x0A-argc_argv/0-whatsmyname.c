#include <stdio.h>

/**
  * main - Prints its name
  * @argc: argument count
  * @argv: argument value
  *
  * Return: Always zero
  */
int main(int argc, char *argv[])
{
	printf("My name is: %s\n", argv[0]);

	return (0);
}
